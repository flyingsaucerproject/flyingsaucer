package org.xhtmlrenderer.render;

import java.awt.*;

/**
 * Not sure if this is a hack or not. Images are rather intrinsic.
 * Need to resolve how replaced elements are repesented.
 */
public class ImageBox extends BlockBox {
    public Image img;
}
